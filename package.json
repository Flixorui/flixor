{
  "name": "flixor",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build:core": "npm run build --workspace=@flixor/core",
    "dev:core": "npm run dev --workspace=@flixor/core"
  }
}
